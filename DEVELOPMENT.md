# 开发状态与后续路线

## 三阶段交付状态

### 第一阶段：扫描导入闭环（已完成）

- PDF 和常见图片解码。
- 纵向 A4 自动拆成上下两张 A5。
- A5 自动旋转、二维码身份解析、四角定位和透视校正。
- 依据不可变 manifest 提取 ROI 并完成五类纸面分类。

### 第二阶段：复核与持久化（已完成）

- SQLite 保存班级、当前学生、周批次、日期、表单快照、导入和观察记录。
- 原始纸面分类与最终业务状态分开保存。
- 涂黑、不确定和低置信度记录进入人工复核。
- 未扫描或仍有待复核项的批次不能确认。

### 第三阶段：汇总与导出（已完成）

- 每名学生、每科缺交次数和总次数。
- 日期、学科、学生和纸面来源组成的缺交明细。
- 模板列名驱动的平台导入工作表。
- 浏览器端完整流程和端到端自动化测试。

## 业务流程

1. 周末建立下周批次，默认选择下一个星期一至星期五。
2. 选择班级和学科，生成周表 PDF 与不可变 manifest。
3. 周一发放给学科代表，周五回收。
4. 导入整张 A4、裁切后的 A5、照片或 PDF。
5. 二维码识别身份，四角定位校正，逐格 OMR。
6. 涂黑和低置信度项目必须人工复核。
7. 确认批次后生成周汇总、缺交明细和平台适配 Excel。

## 数据表

- `classes`：班级配置。
- `students`：当前花名册。
- `homework_weeks`：开始日期、结束日期和状态。
- `homework_week_dates`：本批次实际纳入的日期。
- `homework_forms`：学科、表单 ID、二维码身份、名单和版式快照。
- `homework_imports`：每次扫描来源。
- `homework_observations`：自动分类、置信度、特征和人工结论。
- `homework_notes`：学科代表备注。
- `homework_exports`：导出版本和来源摘要。

已经签发的表使用名单快照，后续改名或转班不会改写历史纸表。

## OMR 语义

- `/` 或 `\`：已交。
- 空白：缺交。
- `X`：从已交更正为缺交。
- 高密度涂黑或形状不确定：人工复核。

数据库同时保存原始纸面类别和最终业务状态。空白与 X 都映射为“缺交”，但不会丢失二者的来源差别。

## Excel 导出

- `周汇总`：每名学生每科缺交次数和总次数。
- `缺交明细`：日期、学科、学号、姓名、状态、原始识别和备注。
- `平台导入`：由用户提供的平台模板第一行驱动列名和顺序，不猜测第三方平台的私有格式。

## 代码边界

- `roster.py` 只负责输入名单，不处理业务状态。
- `forms.py` 只负责冻结版式、生成 PDF 和 manifest。
- `scanning.py` 负责文件解码、拆分、二维码与识别编排。
- `omr/recognize.py` 只负责几何校正和纸面分类。
- `database.py` 保存批次、观察值和人工结论。
- `reporting.py` 将已确认业务结果转换为 Excel。

OMR 分类器不直接写数据库，也不把“缺交”隐式转换为积分。

## 后续增强

- 用真实扫描样本建立回归数据集，按扫描设备校准阈值。
- 人工复核界面增加对应格子的放大图和整表定位联动。
- 为阴影、折痕、低对比度和定位点破损增加质量评分。
- 增加数据库备份、批次撤回和复核审计人字段。
- 根据用户提供的真实平台模板增加经过验证的专用适配器。
